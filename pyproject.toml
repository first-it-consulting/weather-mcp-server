[project]
name = "weather-mcp-server"
version = "0.1.0"
description = "A Model Context Protocol (MCP) server that provides weather information using the WeatherAPI service"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "httpx>=0.28.1",
]
